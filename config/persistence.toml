# Checkpoint and resume defaults.

[persistence]
checkpoint_every = 0
checkpoint_keep = 5
checkpoint_format = "h5"

# Empty means no startup engram load. May be a checkpoint file or a run folder.
load_engram = ""
