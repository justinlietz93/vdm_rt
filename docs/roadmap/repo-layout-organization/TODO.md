# Repo Organization Cleanup TODO

1. [ ] - config/ layout should reflect the codebase layout, with config files existing under folders with names matching the files they are responsible for
2. [ ] - core/ layout should be reorganized to reflect the logical flow of the system
3. [ ] - replace requirements.txt with pyproject.toml and add install options for the base installs to just run the runtime, and then add an option for dev which has additional installs for data analysis and such