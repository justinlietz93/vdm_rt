# VDM Selector Trace Probe Suite

Fresh 1k VDM selector-trace experiment using bursted H5 save/reload.

Goal: keep the current actuator-trace abstraction, train/settle on a stable sentence field for 1000 ticks, then introduce controlled input classes and measure how the selector trace moves before/without witness release.

The model receives raw sentence text only. Metadata labels are external logging fields.
